import 'package:flutter/material.dart';

class AppColors {
  // COULEURS PRINCIPALES
  static const Color primary = Color(0xFF00C853);      // Vert vif
  static const Color primaryDark = Color(0xFF009624);  // Vert foncé
  static const Color primaryLight = Color(0xFFE8F5E9); // Vert très clair

  // FONDS
  static const Color background = Color(0xFFFFFFFF);   // Blanc pur
  static const Color backgroundGrey = Color(0xFFF8F9FA); // Gris très clair
  static const Color cardBg = Color(0xFFFFFFFF);       // Blanc

  // TEXTES
  static const Color textDark = Color(0xFF1A1A2E);     // Noir doux
  static const Color textMedium = Color(0xFF4A4A6A);   // Gris moyen
  static const Color textLight = Color(0xFF9E9E9E);    // Gris clair

  // ACCENTS
  static const Color accent = Color(0xFF00C853);       // Vert
  static const Color danger = Color(0xFFE53935);       // Rouge urgence
  static const Color warning = Color(0xFFFFB300);      // Orange alerte
  static const Color info = Color(0xFF1E88E5);         // Bleu info

  // AUTRES
  static const Color white = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE0E0E0);
  static const Color shadow = Color(0x1A000000);
}
